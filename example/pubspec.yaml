name: rivium_sync_example
description: "RiviumSync Flutter SDK Example - Demonstrates all features of the RiviumSync realtime database SDK"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # RiviumSync SDK - local path for development
  rivium_sync:
    path: ../


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
